# 故障排查与测试报告

## 1. 测试体系概览

针对论坛系统“所有按钮失效”的问题，我们建立了以下三层测试体系：

### 1.1 前端单元测试 (Vitest)
- **位置**: `frontend/src/views/__tests__/LoginView.spec.ts`
- **覆盖内容**: 
  - 登录表单的渲染。
  - 登录按钮的点击事件模拟。
  - 验证按钮点击后是否正确触发 Pinia Store 的 `login` 动作。
  - 验证加载状态下的按钮表现。
- **结果**: 测试通过，证明组件层面的按钮交互逻辑是正常的。

### 1.2 后端 API 接口测试 (Pytest)
- **位置**: `app/tests/test_button_apis_v2.py`
- **覆盖内容**:
  - 用户注册与登录流程。
  - 创建角色 (Persona) 接口 (对应“创建角色”按钮)。
  - 创建论坛 (Forum) 接口 (对应“创建讨论”按钮)。
  - 启动论坛接口 (对应“开始”按钮)。
  - 删除论坛接口 (对应“删除”按钮)。
- **发现问题**: 
  - 普通用户尝试创建 `is_public=True` 的角色时会收到 403 错误。
  - 启动论坛后状态更新存在延迟，需等待后台任务完成。
- **修复**: 调整测试用例以符合权限模型，并增加异步等待机制。

### 1.3 端到端 (E2E) 测试 (Cypress)
- **位置**: `frontend/cypress/e2e/button_smoke.cy.ts`
- **覆盖内容**:
  - 完整的用户操作路径：访问 -> 注册 -> 登录 -> 创建角色 -> 创建论坛 -> 退出。
  - 验证页面跳转和关键 UI 元素（模态框、按钮）的可见性。

## 2. 故障排查发现与修复

在建立测试体系的过程中，我们发现了导致“按钮失效”现象的潜在根源：

### 2.1 前端 Promise 处理错误
- **问题**: 在 `ForumDetailView.vue` 中，`personaStore.fetchPersonas` 动作被调用时，由于该动作在某些情况下（如 Mock 环境或特定错误路径）返回 `undefined`，导致链式调用的 `.catch()` 方法抛出 `TypeError: Cannot read properties of undefined (reading 'catch')`。这会导致组件脚本执行中断，使得页面上的按钮看起来无响应。
- **修复**: 将链式调用改为 `try-catch` 块包裹的 `await` 调用，提高了代码的健壮性。

### 2.2 权限导致的“静默失败”
- **问题**: 普通用户在创建角色时，如果默认勾选了“公开”选项，后端会返回 403 Forbidden。如果前端没有完善的错误提示，用户点击“创建”按钮后会感觉没有任何反应。
- **建议**: 前端应在非管理员用户界面隐藏或禁用“公开”选项，或提供明确的权限错误提示。

### 2.3 数据库锁定 (SQLite)
- **问题**: 在高并发或快速连续操作（如测试环境）下，SQLite 数据库可能会出现 `SQLITE_BUSY_RECOVERY` 锁定，导致写入操作失败。这表现为用户点击按钮后数据没有更新。
- **建议**: 生产环境建议迁移至 PostgreSQL，或优化 SQLite 的连接与重试机制。

## 3. 测试覆盖率与下一步计划

目前关键的交互路径已覆盖。建议后续：
1. **完善 E2E 测试**: 在 CI/CD 环境中配置 Cypress 运行环境。
2. **前端错误边界**: 在 `App.vue` 或路由层添加全局错误边界 (Error Boundary)，捕获未处理的 JS 错误，防止整个页面崩溃。
3. **优化 UX**: 针对 403 权限错误添加更友好的 UI 反馈。
